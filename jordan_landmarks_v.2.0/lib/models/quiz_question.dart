class QuizQuestion {
  String question;
  List<String> choices;
  int correctIndex;
  String difficulty;
  String explanation;

  QuizQuestion(this.question, this.choices, this.correctIndex, this.difficulty, this.explanation);
}
